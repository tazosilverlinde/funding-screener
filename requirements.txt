streamlit>=1.32
streamlit-autorefresh>=1.0.1
httpx>=0.27
pandas>=2.2
pydantic>=2.6
pyyaml>=6.0
pytest>=8.0
rich>=13.7
# Liquidation-tape WebSocket consumer (Binance forceOrder stream).
websockets>=12.0
# Process memory instrumentation (Round 51) — surfaces RSS in the perf expander
# against the 2GB per-process budget.
psutil>=5.9
# Type-checking on the most-touched pure modules (Round 65). Runs in CI on
# signals.py + thesis.py; everything else is opt-in via mypy.ini's strict_files.
mypy>=1.10
