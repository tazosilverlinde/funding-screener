# funding_screener does not currently use any environment variables — public
# REST endpoints only. This file is a placeholder so future contributors know
# where to wire up new flags (e.g., a custom HTTP proxy) if needed.
#
# Examples (none of these are read today):
#   HTTPS_PROXY=http://localhost:8888
#   FUNDING_SCREENER_LOG_LEVEL=INFO
